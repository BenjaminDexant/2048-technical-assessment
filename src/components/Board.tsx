import React from "react";

const Board: React.FC = () => {
    return (
        <div>
            This is where the 4 x 4 grid will be initialized and displayed as a game board
        </div>
    );
}

export default Board;
